<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FF6200EE"/> <!-- Example color -->
    <corners android:radius="4dp"/> <!-- Optional: Adjust corner radius -->
    <!-- Optional: Define padding -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
    <!-- Optional: Add stroke -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF"/>
</shape>
